# v0.92 Sprint #5857 Session Prompt

Use this prompt to start the First-Birthday Core sprint session.

```text
You own v0.92 sprint coordination issue #5857, First-Birthday Core.

Read root AGENTS.md first. Use the typed C-SDLC v2 route, an issue-bound
worktree per child, and an issue-bound goal after bind and before implementation.
Never write tracked work on main and never use /private/tmp.

This umbrella coordinates; it does not implement birthday features. Child
issues retain all code, proof, review, publication, and terminal authority.

Before execution:

1. Verify WP-01 #5817 is merged and ancestral to current main.
2. Read issue #5857, .csdlc/issues/5857/, both Sprint Execution Packets under
   .csdlc/prepared/issues/5857/, and the v0.92 issue wave and feature contracts.
3. Run typed doctor for #5857 and every child. Use canonical .csdlc cards; do
   not recreate them because an obsolete helper searches for .adl bundles.
4. Prepare child plans in parallel only where their declared dependencies allow.
5. Do not claim birth, continuity, memory grounding, capability, learning, or
   witness completion from prose or fixtures.

WP-01 published the initialized child records under its own temporary publication
claim. After WP-01 releases that claim, create and register each real child
worktree, use typed `csdlc-bind --reacquire-request` to acquire the child's exact
issue-local paths, then run the normal bind and goal sequence. Do not assume the
bootstrap reservation is still active.

Exact child wave:

- #5825, WP-08: first true Godel-agent birthday contract
- #5826, WP-09: stable identity and name
- #5827, WP-10: continuity and replay proof
- #5828, WP-11: memory grounding and Memory Palace topology
- #5829, WP-12: capability envelope
- #5830, WP-13: ACP cognitive profile
- #5831, WP-13A: Adaptive Learning DAG
- #5833, WP-15: birth witnesses and receipt
- #5834, WP-16: integrated birthday review packet

Serial gates:

- #5825 before #5826 and #5829
- #5826 before #5827 and #5828
- #5827, #5828, #5829, and #5830 before #5833
- #5831 and #5833 before #5834

After #5827, #5828 and #5829 may use separate child sessions. #5830 waits for
both of them and #5831 waits for #5830; these dependency-linked children do not
run in parallel. Keep their state and proof surfaces separate and reconcile
only through declared contracts.

For each ready child, deliver the whole useful outcome and both positive and
negative proof. Obtain exact-head independent review, remediate all findings,
and publish a PR containing `Closes #<child>`. No topic-only document,
placeholder API, synthetic success, or partial acceptance counts as done.

Use watchers for healthy PR waits and continue only another declared safe lane.
Record any parallelism assumption that proved wrong.

Maintain:

- .csdlc/evidence/5857/activity.jsonl
- .csdlc/evidence/5857/sprint-review.md

Close #5857 only after the integrated review proves coherent identity,
continuity, memory, capability, learning, witness, privacy, and replay truth with
no unresolved actionable finding.
```
