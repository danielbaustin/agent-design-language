## Summary

Execute **WP-03** for v0.92 within this issue's approved design and ownership boundary.

Deliver one supported browser-trusted localhost HTTPS flow for the separate Observatory and Runtime API, including atomic certificate handling, explicit operator trust, consistent configuration, and reproducible positive, negative, and platform proof.

## Required Outcome

browser-trusted local Observatory HTTPS with reproducible browser and health proof

## Deliverables

- Supported localhost certificate generation, validation, trust installation, and reissue flow
- Consistent Runtime API and separate Observatory HTTPS configuration and startup guidance
- Chrome, curl, HTML, health/readiness, Runtime-feed, and negative certificate evidence
- Platform disposition for macOS, Linux, and native Windows without inferred portability

## Dependencies

- WP-01 issue 5817 merged and ancestral
- WP-02A proving substrate terminal before implementation
- Sprint 5855 coordination
- Serialization with issue 5820 for shared Runtime init or TLS paths

## Owned Paths

- `adl-runtime/src/local_tls.rs`
- `adl-runtime/src/bin/adl-runtime-local-tls-bootstrap.rs`
- `adl-runtime/tests/local_tls.rs`
- `demos/html-observatory/runtime-v3.config.json`
- `demos/html-observatory/README.md`
- `adl/tools/validate_v092_browser_trusted_observatory.mjs`

## Validation And Proof

- Prove generation, SAN, expiry, Rustls pair, permissions, atomic replacement, and last-valid-pair negatives.
- Use the issue-delivered Playwright validator to open real Chrome against the live HTTPS Observatory and Runtime, reject interstitials or TLS console/network errors, and prove HTML, health, readiness, and feed access with browser-visible evidence.
- Independently verify the live Runtime HTTPS identity and endpoint responses without substituting curl for browser trust.
- Run the same issue-delivered browser validator on native macOS, Linux, and Windows evidence lanes and fail closed on any missing required platform disposition.
- Reject unrelated changes and support exact-head review.

## Acceptance Criteria

- Chrome loads the supported Observatory URL without certificate warnings or bypasses
- The committed certificate pair has correct localhost SANs, validity, Rustls compatibility, and private-key permissions
- Runtime API, separate Observatory server, configured origins, docs, URLs, and probes consistently use HTTPS
- Verified curl and browser paths reach Observatory HTML plus Runtime health, readiness, and feed
- Missing, malformed, expired, mismatched, untrusted, or partial certificate states fail truthfully and preserve the last valid pair
- No private key, token, trust-store export, or secret is committed or logged
- macOS proof is retained and Linux/native Windows behavior is implemented or explicitly blocked with evidence
- One exact-head review has no unresolved actionable findings

## Non-Goals

- AWS, ACM, Route53, or Let's Encrypt issuance for localhost
- Public production-domain certificate automation
- Disabling TLS verification, browser warnings, or CORS policy
- Serving Observatory HTML from Runtime or redesigning the Observatory
- Claiming WP-03, WP-14, or WP-18A completion

## Execution Boundary

This issue is design-ready, not implemented. Execution must reverify terminal dependencies, reacquire a just-in-time claim for the exact owned paths, retain the declared positive and negative proof, complete exact-head review, and use `Closes #5800` in its implementation PR.

<!-- csdlc-github-operation:v092-execution-ready-body-5800-20260805 -->
