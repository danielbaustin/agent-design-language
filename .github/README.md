# GitHub Metadata

This directory contains GitHub repository metadata for ADL, including Actions
workflow definitions and future issue or pull request templates.

It is intentionally tracked as the top-level `.github/` directory. Do not move
review evidence, local workflow state, or ignored `.adl/` artifacts here.
