# ADL Schemas

Canonical location for published schema artifacts.

- Place JSON Schema (and future schema profiles) here.
- Examples remain under `adl-spec/examples/`.
- The v0.2 schema draft is described in `adl-spec/ADL_v0.2_Schema_Extensions.md`.
