# ObsMem Transition Memory

This packet is the bounded proof surface for `WP-08`.

It promotes durable transition-memory inputs from tracked review/evidence
surfaces rather than local `.adl` state:

- promoted transition outcome truth
- tracked review synthesis
- tracked signed trace proof
- explicit follow-on visibility

Primary operator entrypoint:

```bash
bash adl/tools/test_v0914_obsmem_transition_memory.sh
```

Primary reviewer packet:

- `OBSMEM_TRANSITION_MEMORY_PACKET_v0.91.4.md`
