# Structured Output Record Records

Tracked Structured Output Record records belong here once they are ready to serve as the public execution record for a task.

The local `.adl/` output record may be used during drafting or iterative execution, but `pr finish`-style lifecycle completion should promote the canonical SOR here before the result is treated as closed and auditable.
