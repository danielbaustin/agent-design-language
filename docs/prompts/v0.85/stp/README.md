# Structured Task Prompt Records

Tracked Structured Task Prompt records belong here once they are ready to serve as the public source of truth for:

- issue creation or reconciliation
- milestone/work-package traceability
- editor-visible prompt history

The local `.adl/` draft version may exist first, but `pr create`-style lifecycle transitions should promote the canonical STP here before treating it as authoritative.
