# V0.91.6 Integrated Runtime Soak Proof for #4245

This packet records the first durable reviewer-facing integrated runtime soak for `#4245`.

## Scope

The direct proof surface for this issue is the tracked artifact tree generated by:

```bash
cargo run --manifest-path adl/Cargo.toml --bin run_v0916_integrated_runtime_soak -- \
  --out docs/milestones/v0.91.6/review/runtime/v0916_integrated_runtime_soak_4245
```

Generated artifact root:

- `docs/milestones/v0.91.6/review/runtime/v0916_integrated_runtime_soak_4245/`

## What this proves

- the long-lived runtime proves a main run/restart/inspection continuity path plus a companion live stop-between-cycles probe while preserving durable cycle and continuity state
- the stop proof is live: one cycle completes, stop is issued during the cadence sleep window, and later cycles do not start
- the resilience substrate emits tracked timeout, bulkhead saturation/backpressure, and degraded fallback traces through the real Rust entrypoints
- remote-exec timeout classification is live against a hanging local endpoint and records stable failure kind plus retryability
- one tracked ObsMem handoff path still builds a structured transition-memory request
- one explicit injected-lease contract probe verifies leased-state and overlap-blocking behavior without claiming a concurrently running live lease owner
- the full reviewer-facing artifact set passes a basic public-artifact safety scan after all top-level proof outputs are written

## What this does not prove

- full `v0.92` activation readiness
- full Observatory or Unity UI completion
- end-to-end ACIP runtime execution beyond prerequisite consumption
- external-agent trust, signing, or transport closure
- always-on autonomy or broad runtime coherence beyond the exact surfaces exercised here

## Prerequisite evidence consumed

These milestone surfaces are treated as prerequisites or consumed inputs rather than re-executed inside this soak runner:

- `docs/milestones/v0.91.6/features/TOKIO_RUNTIME_SUBSTRATE_v0.91.6.md`
- `docs/milestones/v0.91.6/features/ACIP_A2A_PROVIDER_COMMUNICATIONS_v0.91.6.md`
- `docs/milestones/v0.91.6/features/AEE_MEMORY_ACP_BRIDGE_ACCOUNTING_v0.91.6.md`
- `docs/milestones/v0.91.6/features/OBSERVATORY_UNITY_CONSUMPTION_CLASSIFICATION_v0.91.6.md`

## Reviewer path

1. Inspect `v0916_integrated_runtime_soak_4245/integrated_runtime_soak_proof.json`.
2. Inspect `v0916_integrated_runtime_soak_4245/long_lived_agent/state/status.json` and `.../cycle_ledger.jsonl`.
3. Inspect `v0916_integrated_runtime_soak_4245/inspection/latest.json`.
4. Inspect `v0916_integrated_runtime_soak_4245/long_lived_agent_stop_probe/stop_probe.json`.
5. Inspect `v0916_integrated_runtime_soak_4245/resilience/timeout_execution.json`.
6. Inspect `v0916_integrated_runtime_soak_4245/resilience/bulkhead_execution.json`.
7. Inspect `v0916_integrated_runtime_soak_4245/resilience/degraded_fallback_execution.json`.
8. Inspect `v0916_integrated_runtime_soak_4245/remote_exec/timeout_probe.json`.
9. Inspect `v0916_integrated_runtime_soak_4245/obsmem/transition_memory_request.json`.
10. Inspect `v0916_integrated_runtime_soak_4245/audit/artifact_safety_scan.json`.

## Acceptance mapping

| Acceptance surface | Evidence |
| --- | --- |
| restart | `long_lived_agent/restart_status_cycle3.json` plus append-only `state/cycle_ledger.jsonl` |
| live stop between cycles | `long_lived_agent_stop_probe/stop_probe.json` |
| timeout | `resilience/timeout_execution.json` and `remote_exec/timeout_probe.json` |
| backpressure | `resilience/bulkhead_execution.json` |
| degraded mode | `resilience/degraded_fallback_execution.json` |
| handoff path | `obsmem/transition_memory_request.json` |
| status snapshots | `long_lived_agent/initial_status.json`, `run_status_cycle2.json`, `status_after_stop.json`, and `inspection/latest.json` |
| injected lease contract | `long_lived_agent/lease_injection_probe.json` |

## Truth note

This issue intentionally proves one integrated runtime slice with durable artifacts. It does not claim that every planned runtime component is already unified into a final always-on system.
