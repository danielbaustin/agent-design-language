# ADL Feature List

The durable tracked ADL feature list now lives at:

- `docs/planning/ADL_FEATURE_LIST.md`

This milestone-local file is retained as a compatibility pointer because the
feature list was originally recovered from the v0.75 planning surface.

Use the planning copy for ongoing milestone-by-milestone updates, including:

- current implementation status
- implemented features missing from older narratives
- target completion milestones through `v0.95`
- deferred/optional feature decisions
