# Retired Five-Command Editing Demo

The historical v0.85 five-command demo depended on the removed v1 lifecycle
wrappers. Its executable now fails closed and directs operators to
`csdlc-install resolve` and the independent typed C-SDLC v2 binaries.

This page preserves the name for inbound links; it is not an operational
workflow or compatibility promise. Historical proof artifacts remain unchanged.
